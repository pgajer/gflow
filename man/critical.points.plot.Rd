% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2d_smooth_morse_smale.R
\name{critical.points.plot}
\alias{critical.points.plot}
\title{Plot Critical Points}
\usage{
critical.points.plot(
  critical_points,
  xlim = c(0, 1),
  ylim = c(0, 1),
  main = "Critical Points Analysis",
  add = FALSE,
  ...
)
}
\arguments{
\item{critical_points}{List with maxima, minima, and saddles matrices}

\item{xlim}{X-axis limits}

\item{ylim}{Y-axis limits}

\item{main}{Plot title}

\item{add}{Logical, whether to add to existing plot}

\item{...}{Additional arguments passed to plot}
}
\value{
Invisible NULL
}
\description{
Plots critical points (maxima, minima, saddles) from continuous analysis.
}
\examples{
# Create a function with known critical points
f <- function(x, y) x^2 - y^2  # Saddle at origin
gradient <- function(x, y) c(2*x, -2*y)
grid <- create.grid(30)
critical <- find.critical.points.continuous(gradient, grid)
critical.points.plot(critical)
}
