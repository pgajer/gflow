% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_utils.R
\name{show.3d.cl}
\alias{show.3d.cl}
\title{Highlight a Specific Cluster in 3D Space}
\usage{
show.3d.cl(
  cl,
  cltr,
  X,
  cl.radius = 1e-04,
  show.ref.cltr = TRUE,
  show.labels = FALSE,
  adj = c(1.3, 0),
  ...
)
}
\arguments{
\item{cl}{Cluster ID to highlight.}

\item{cltr}{Vector of cluster IDs of length nrow(X).}

\item{X}{A matrix or data.frame with 3 columns representing 3D coordinates.}

\item{cl.radius}{Radius of spheres for the highlighted cluster.}

\item{show.ref.cltr}{Logical. Whether to show non-highlighted clusters.}

\item{show.labels}{Logical. Whether to show row names of highlighted points.}

\item{adj}{Adjustment parameter for text positioning.}

\item{...}{Additional arguments.}
}
\value{
Invisibly returns NULL. The function is called for its side effect.
}
\description{
Shows a specified cluster with emphasis while displaying other clusters in gray
}
\details{
This function creates a 3D plot where one specific cluster is highlighted in red
while all other clusters are shown in gray. Optionally, labels can be shown for
the highlighted cluster points.
}
\examples{
\dontrun{
X <- matrix(rnorm(300), ncol = 3)
cltr <- sample(1:5, 100, replace = TRUE)

# Highlight cluster 3
show.3d.cl(3, cltr, X)

# Show with labels
rownames(X) <- paste0("Point", 1:100)
show.3d.cl(3, cltr, X, show.labels = TRUE)
}

}
