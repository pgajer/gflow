% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boxcox_utils.R
\name{boxcox.transform}
\alias{boxcox.transform}
\title{Box-Cox power transform}
\usage{
boxcox.transform(y, lambda)
}
\arguments{
\item{y}{A numeric vector of strictly positive and finite values.}

\item{lambda}{A numeric scalar giving the power parameter \eqn{\lambda}.}
}
\value{
A numeric vector of the same length as \code{y}, containing
the transformed values.
}
\description{
Applies the Box-Cox transform to a positive numeric vector \eqn{y}.
Uses \eqn{\log(y)} when \eqn{\lambda \approx 0}, and
\eqn{(y^\lambda - 1)/\lambda} otherwise.
}
\details{
The transform is defined as
\deqn{
  T_\lambda(y) =
  \begin{cases}
    \dfrac{y^\lambda - 1}{\lambda}, & \lambda \neq 0, \\
    \log(y),                         & \lambda = 0.
  \end{cases}
}
This implementation switches to the log form when \eqn{|\lambda| < 10^{-8}}.
}
\examples{
y <- rexp(50, rate = 2)          # positive data
boxcox.transform(y, lambda = 0)  # log-transform
boxcox.transform(y, lambda = 0.5)

}
\seealso{
\code{\link{boxcox.mle}}
}
