% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_kmean.R
\name{univariate.gkmm}
\alias{univariate.gkmm}
\title{Adaptive Neighborhood Size Graph K-Means for Univariate Data}
\usage{
univariate.gkmm(
  x,
  y,
  y.true = NULL,
  use.median = FALSE,
  h.min = 2,
  h.max = min(30, length(x)),
  n.CVs = 1000,
  n.CV.folds = 10,
  p = 0.95,
  n.bb = 500,
  ikernel = 1,
  n.cores = 1,
  dist.normalization.factor = 1.01,
  epsilon = 1e-15,
  seed = NULL
)
}
\arguments{
\item{x}{Numeric vector of predictor values.}

\item{y}{Numeric vector of response values.}

\item{y.true}{Optional numeric vector of true response values for performance
evaluation.}

\item{use.median}{Logical; if TRUE uses median, if FALSE uses mean for
predictions. Default is FALSE.}

\item{h.min}{Minimum neighborhood size to consider (>= 2). Default is 2.}

\item{h.max}{Maximum neighborhood size to consider. Default is
min(30, length(x)).}

\item{n.CVs}{Number of cross-validation iterations. Default is 1000.}

\item{n.CV.folds}{Number of CV folds (>= 2). Default is 10.}

\item{p}{Probability level for credible intervals (0 < p < 1). Default is
0.95.}

\item{n.bb}{Number of Bayesian bootstrap iterations (0 to skip). Default is
500.}

\item{ikernel}{Integer specifying kernel function (1-6). Default is 1.}

\item{n.cores}{Number of CPU cores for parallel processing. Default is 1.}

\item{dist.normalization.factor}{Distance normalization factor (> 1).
Default is 1.01.}

\item{epsilon}{Small positive value for numerical stability. Default is
1e-15.}

\item{seed}{Random seed for reproducibility. Default is NULL.}
}
\value{
An object of class "ugkmm" containing:
\item{h_values}{Vector of tested neighborhood sizes}
\item{h_cv_errors}{Cross-validation errors for each h}
\item{mean_cv_error}{Mean CV error across all h values}
\item{opt_h}{Optimal neighborhood size}
\item{predictions}{Fitted values}
\item{bb_predictions}{Bootstrap central tendency estimates}
\item{opt_ci_lower}{Lower credible interval bounds}
\item{opt_ci_upper}{Upper credible interval bounds}
\item{x_sorted}{Sorted predictor values}
\item{y_sorted}{Response values (sorted by x)}
\item{y_true_sorted}{True values (sorted by x) if provided}
}
\description{
Performs nonparametric regression using graph-based k-means with adaptive
neighborhood size selection. The method constructs chain graphs from sorted
predictor values and uses cross-validation to select the optimal
neighborhood size.
}
\details{
The algorithm:
\enumerate{
\item Sorts data by predictor values
\item Constructs chain graphs with varying neighborhood sizes
\item Uses cross-validation to select optimal neighborhood size
\item Computes predictions using kernel-weighted means
\item Optionally performs Bayesian bootstrap for uncertainty quantification
}
}
\examples{
\dontrun{
# Generate example data
set.seed(123)
x <- seq(0, 10, length.out = 100)
y <- sin(x) + rnorm(100, 0, 0.1)

# Fit model
result <- univariate.gkmm(x, y, h.min = 2, h.max = 20, n.bb = 100)

# Plot results
plot(result)

# Summary
summary(result)
}

}
\seealso{
\code{\link{plot.ugkmm}} for plotting methods
\code{\link{summary.ugkmm}} for summary statistics
}
