% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2d_smooth_morse_smale.R
\name{plot.morse.smale.trajectories.from.point}
\alias{plot.morse.smale.trajectories.from.point}
\title{Plot Morse-Smale Trajectories from Point}
\usage{
\method{plot}{morse.smale.trajectories.from.point}(
  x,
  y,
  mixture,
  grid,
  step = 0.01,
  show.arrows = TRUE,
  use.custom.legend = FALSE,
  ...
)
}
\arguments{
\item{x}{Starting x coordinate}

\item{y}{Starting y coordinate}

\item{mixture}{Object containing:
\itemize{
\item f: Function taking (x, y) and returning scalar value
\item gradient: Function taking (x, y) and returning gradient vector c(dx, dy)
}}

\item{grid}{Grid object created by create.grid for contour plot background}

\item{step}{Step size for gradient flow integration (default 0.01)}

\item{show.arrows}{Logical, whether to show directional arrows on trajectories}

\item{use.custom.legend}{Logical, whether to use custom legend.}

\item{...}{Additional arguments passed to function.contours.plot}
}
\value{
Invisible list containing:
\item{ascending}{Matrix of points along ascending trajectory}
\item{descending}{Matrix of points along descending trajectory}
}
\description{
Visualizes gradient flow trajectories (both ascending and descending)
from a specific starting point. Shows the trajectories on a contour plot background,
marks the starting point and endpoints (critical points), and optionally adds
directional arrows along the trajectories.
}
\examples{
\dontrun{
grid <- create.grid(50)
mixture <- list(
  f = function(x, y) -((x-0.3)^2 + (y-0.7)^2) - 2*((x-0.7)^2 + (y-0.3)^2),
  gradient = function(x, y) {
    c(-2*(x-0.3) - 4*(x-0.7), -2*(y-0.7) - 4*(y-0.3))
  }
)
# Plot trajectories from a point
result <- plot.morse.smale.trajectories.from.point(0.5, 0.5, mixture, grid)
}
}
