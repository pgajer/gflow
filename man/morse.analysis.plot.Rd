% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2d_smooth_morse_smale.R
\name{morse.analysis.plot}
\alias{morse.analysis.plot}
\title{Plot Morse Analysis}
\usage{
morse.analysis.plot(
  grid,
  f.grid,
  trajectory,
  critical.points,
  show.contours = TRUE,
  main = "Morse Analysis",
  ...
)
}
\arguments{
\item{grid}{Grid object from create.grid}

\item{f.grid}{Matrix of function values}

\item{trajectory}{Trajectory object from compute.gradient.trajectory}

\item{critical.points}{List with critical points from find.critical.points}

\item{show.contours}{Logical, whether to show contour lines (default TRUE)}

\item{main}{Plot title}

\item{...}{Additional arguments passed to plotting functions}
}
\value{
Invisible NULL
}
\description{
Combined plot showing function contours, critical points, and a gradient trajectory.
}
\examples{
\dontrun{
grid <- create.grid(30)
f <- function(x, y) sin(3*x) * cos(3*y)
f.grid <- evaluate.function.on.grid(f, grid)
critical <- find.critical.points(f.grid)
traj <- compute.gradient.trajectory(15, 20, f.grid)
morse.analysis.plot(grid, f.grid, traj, critical)
}
}
