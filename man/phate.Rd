% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phate_core.R
\name{phate}
\alias{phate}
\title{End-to-End PHATE (Core + Embedding)}
\usage{
phate(
  X = NULL,
  D = NULL,
  K = NULL,
  P = NULL,
  k = 15L,
  alpha.decay = 40,
  knn.use = TRUE,
  knn.symmetrize = c("mean", "max"),
  t = "auto",
  t.max = 50L,
  pca.dim = NULL,
  pca.center = TRUE,
  pca.scale = FALSE,
  kernel.eps = 1e-12,
  potential.eps = 1e-12,
  compute.D.pot = FALSE,
  ndim = 2L,
  embed.method = c("metric_mds", "nonmetric_mds"),
  embed.maxit = 200L,
  embed.tol = 0.001,
  cmdscale.add = TRUE,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{X, D, K, P}{Inputs passed to \code{\link{phate.core}} (provide exactly one).}

\item{k, alpha.decay, knn.use, knn.symmetrize, t, t.max, pca.dim, pca.center, pca.scale, }{kernel.eps,potential.eps,compute.D.pot Passed to \code{\link{phate.core}}.}

\item{ndim}{Embedding dimension passed to \code{\link{phate.embed}}.}

\item{embed.method}{Embedding method passed to \code{\link{phate.embed}}.}

\item{embed.maxit, embed.tol, cmdscale.add, seed}{Passed to \code{\link{phate.embed}}.}

\item{verbose}{Logical; print progress.}
}
\value{
A list of class \code{"phate"} with components:
\describe{
\item{embedding}{Final low-dimensional coordinates.}
\item{core}{Object from \code{\link{phate.core}}.}
\item{embed}{Object from \code{\link{phate.embed}}.}
\item{t}{Diffusion time used in the core step.}
\item{call}{Matched call.}
}
}
\description{
One-call wrapper that runs \code{\link{phate.core}} followed by
\code{\link{phate.embed}}.
}
\examples{
\dontrun{
set.seed(1)
X <- matrix(rnorm(150), ncol = 5)
fit <- phate(X = X, k = 10, t = "auto", ndim = 2)
fit$embedding[1:5, ]
fit$t
}

}
