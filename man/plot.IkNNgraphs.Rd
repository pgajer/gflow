% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iknn_graphs.R
\name{plot.IkNNgraphs}
\alias{plot.IkNNgraphs}
\title{Plot Diagnostics for Intersection k-NN Graph Analysis}
\usage{
\method{plot}{IkNNgraphs}(
  x,
  type = "diag",
  diags = c("edist", "edge", "deg"),
  with.pwlm = TRUE,
  with.lmin = FALSE,
  breakpoint.col = "blue",
  lmin.col = "gray",
  k = NA,
  mar = c(2.5, 2.5, 0.5, 0.5),
  mgp = c(2.5, 0.5, 0),
  tcl = -0.3,
  xline = 2.4,
  yline = 3.15,
  ...
)
}
\arguments{
\item{x}{A list containing analysis results for intersection k-NN graphs, typically
with components like 'k.values', 'edit.distances', 'n.edges.in.pruned.graph',
and 'js.div'.}

\item{type}{Character string specifying the type of plot. Either "diag" for diagnostic
plots (default) or "graph" for network visualization.}

\item{diags}{Character vector specifying which diagnostics to show. Options include
"edist" (edit distances), "edge" (edge counts), and "deg" (degree distribution).
Default is c("edist", "edge", "deg").}

\item{with.pwlm}{Logical. If TRUE, overlays piecewise linear model fits on the plots.
Default is TRUE.}

\item{with.lmin}{Logical. If TRUE, shows vertical lines at local minimum points.
Default is FALSE.}

\item{breakpoint.col}{Color for breakpoint vertical lines. Default is "blue".}

\item{lmin.col}{Color for local minima vertical lines. Default is "gray".}

\item{k}{Optional numeric value to highlight a specific k value on the plots.}

\item{mar}{Numeric vector of plot margins (bottom, left, top, right). Default is
c(2.5, 2.5, 0.5, 0.5).}

\item{mgp}{Numeric vector for axis title, labels, and line positions.
Default is c(2.5, 0.5, 0).}

\item{tcl}{Numeric value for tick mark length. Default is -0.3.}

\item{xline}{Numeric value for position of x-axis label. Default is 2.4.}

\item{yline}{Numeric value for position of y-axis label. Default is 3.15.}

\item{...}{Additional arguments passed to plot().}
}
\value{
Invisibly returns NULL. The function is called for its side effect of creating plots.
}
\description{
Creates diagnostic plots for analyzing the properties of intersection k-NN graphs
across different k values. Can display edit distances, edge counts, and
Jensen-Shannon divergence metrics.
}
\examples{
\dontrun{
# Create sample data for IkNNgraphs analysis
set.seed(123)
res <- list(
  k.values = 3:10,
  edit.distances = c(25, 18, 14, 12, 10, 9, 8, 7),
  n.edges.in.pruned.graph = c(50, 80, 100, 115, 125, 132, 138, 142),
  js.div = c(0.5, 0.3, 0.2, 0.15, 0.12, 0.1, 0.09, 0.08),
  edit.distances.breakpoint = 5,
  n.edges.in.pruned.graph.breakpoint = 6,
  js.div.breakpoint = 5.5
)

# Plot diagnostics with default settings
  plot.IkNNgraphs(res)

  # Plot only edit distances and edge counts
  plot.IkNNgraphs(res, diags = c("edist", "edge"))
}

}
