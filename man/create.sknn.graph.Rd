% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sknn_graphs.R
\name{create.sknn.graph}
\alias{create.sknn.graph}
\title{Compute a Symmetric k-Nearest Neighbor Graph}
\usage{
create.sknn.graph(
  X,
  k,
  connect.components = FALSE,
  connect.method = c("component.mst", "global.mst"),
  edge.weight = c("distance")
)
}
\arguments{
\item{X}{Numeric matrix or data frame with observations in rows.}

\item{k}{Integer scalar. Number of non-self nearest neighbors.}

\item{connect.components}{Logical scalar. If \code{TRUE}, add MST bridge edges
so that the final graph is connected whenever possible.}

\item{connect.method}{Character scalar. \code{"component.mst"} adds the minimum
number of shortest inter-component bridges. \code{"global.mst"} unions the
sKNN graph with the Euclidean MST on all vertices.}

\item{edge.weight}{Character scalar. Currently only \code{"distance"} is
supported.}
}
\value{
A list of class \code{"sknn_graph"} with adjacency lists, weights, edge
matrix, kNN index matrix, component diagnostics, and any MST edges added.
}
\description{
Creates an undirected symmetric k-nearest neighbor (sKNN) graph from a numeric
data matrix. An edge between vertices \code{i} and \code{j} is present when either
\code{j} is among the \code{k} nearest neighbors of \code{i}, or \code{i} is among the \code{k}
nearest neighbors of \code{j}.
}
\details{
With no nearest-neighbor ties, the sKNN edge rule is equivalent to
\deqn{d_{ij} \le \max(\sigma_i, \sigma_j),}
where \eqn{\sigma_i} is the distance from point \eqn{i} to its \code{k}-th
non-self nearest neighbor.

If \code{connect.components = TRUE}, the graph is augmented with MST edges. The
default \code{connect.method = "component.mst"} first collapses each connected
component of the sKNN graph to one component vertex, weights each component
pair by the shortest data-space edge between them, and adds the minimum
spanning tree of this component graph. This adds exactly \code{m - 1} bridge
edges when the original sKNN graph has \code{m} connected components. The
alternative \code{connect.method = "global.mst"} unions the sKNN graph with the
full Euclidean MST on the data points.
}
\examples{
X <- rbind(c(0, 0), c(1, 0), c(10, 0), c(11, 0))
g <- create.sknn.graph(X, k = 1, connect.components = TRUE)
g$n_components_before
g$n_components_after
g$mst_edge_matrix

}
