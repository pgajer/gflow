% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gflow_cx_utils.R
\name{analyze.harmonic.extensions}
\alias{analyze.harmonic.extensions}
\title{Analyze Harmonic Extensions for Local Extrema}
\usage{
analyze.harmonic.extensions(
  adj_list,
  weight_list,
  y,
  predictions,
  extrema_df,
  hop_offset = 2,
  min_hop_idx = 0,
  max_hop_idx = Inf,
  methods = c("weighted_mean", "harmonic_iterative", "harmonic_eigen",
    "biharmonic_harmonic", "boundary_smoothed"),
  max_iterations = 100,
  tolerance = 1e-06,
  sigma = 1,
  visualize = TRUE,
  save_results = FALSE,
  output_dir = "harmonic_extension_analysis",
  verbose = TRUE
)
}
\arguments{
\item{adj_list}{Original graph adjacency list}

\item{weight_list}{Original graph weight list}

\item{y}{Original function values on vertices}

\item{predictions}{Smoothed predictions (e.g., from spectral filtering)}

\item{extrema_df}{Data frame containing extrema information}

\item{hop_offset}{Additional hop distance beyond extremum's hop radius (default: 2)}

\item{min_hop_idx}{Minimum hop index for extrema to process (default: 0)}

\item{max_hop_idx}{Maximum hop index for extrema to process (default: Inf)}

\item{methods}{Vector of harmonic extension methods to test}

\item{max_iterations}{Maximum iterations for iterative methods (default: 100)}

\item{tolerance}{Convergence tolerance (default: 1e-6)}

\item{sigma}{Parameter for weighted mean method (default: 1.0)}

\item{visualize}{Whether to visualize results (default: TRUE)}

\item{save_results}{Whether to save detailed results (default: FALSE)}

\item{output_dir}{Directory for saving results (default: "harmonic_extension_analysis")}

\item{verbose}{Whether to print progress messages (default: TRUE)}
}
\value{
A list containing:
\describe{
\item{subgraphs}{List of extracted subgraphs for each extremum}
\item{results}{List of results from applying harmonic extension methods}
\item{metrics}{Data frame with detailed metrics for all extrema and methods}
\item{summary}{Data frame with summary statistics across methods}
\item{extrema_info}{Data frame with information about processed extrema}
}
}
\description{
Complete workflow for analyzing how different harmonic extension methods perform
on local extrema in a graph function. This function extracts subgraphs around
each extremum and applies various harmonic extension methods for comparison.
}
\examples{
\dontrun{
result <- analyze.harmonic.extensions(
  graph$adj_list,
  graph$weight_list,
  y,
  gsf_res$predictions,
  gsf_b_cx$extrema_df,
  hop_offset = 2,
  min_hop_idx = 1,
  max_hop_idx = 5
)
}

}
