% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graphics.R
\name{plot.kh.matrix}
\alias{plot.kh.matrix}
\title{Plot a k-h Matrix}
\usage{
\method{plot}{kh.matrix}(
  x,
  existing.k = NULL,
  h.values = NULL,
  id = NULL,
  color.palette = c("white", "black"),
  xlab = "k (number of nearest neighbors)",
  ylab = "h (hop size)",
  main = NULL,
  with.legend = FALSE,
  ...
)
}
\arguments{
\item{x}{Either a kh.matrix object (list with components kh.mat, existing.k, h.values, id)
or a matrix where rows represent k values and columns represent h values.
Values should be 0 (absent) or 1 (present).}

\item{existing.k}{A vector of k values corresponding to the rows (used when x is a matrix).}

\item{h.values}{A vector of h values corresponding to the columns (used when x is a matrix).}

\item{id}{A string identifier for the local maximum being plotted (used when x is a matrix).}

\item{color.palette}{A vector of two colors for the plot. Default is c("white", "black").}

\item{xlab}{Label for the x-axis. Default is "k (number of nearest neighbors)".}

\item{ylab}{Label for the y-axis. Default is "h (hop size)".}

\item{main}{Title of the plot. Default is "Presence of Local Maximum" followed by id.}

\item{with.legend}{Logical. If TRUE, a legend is added to the plot. Default is FALSE.}

\item{...}{Additional arguments passed to image()}
}
\value{
A plot is created on the current graphics device.
}
\description{
This function creates a heatmap-style plot of a k-h matrix, which represents
the presence or absence of a local maximum for different combinations of
k (number of nearest neighbors) and h (hop size) values.
}
\details{
The function creates a heatmap where each cell represents a combination of k and h values.
Black cells indicate the presence of a local maximum, while white cells indicate its absence.
}
\examples{
\dontrun{
# Create a kh.matrix object
kh.mat <- matrix(c(0,1,1,0,1,0,1,1,0), nrow = 3)
existing.k <- c(10, 20, 30)
h.values <- c(1, 2, 3)
khm <- kh.matrix(kh.mat, existing.k, h.values, id = "LM1")

# Basic plot
plot(khm)

# With custom colors and a legend
plot(khm,
     color.palette = c("lightblue", "darkred"),
     with.legend = TRUE)

# You can also override parameters from the object
plot(khm, main = "Custom Title", xlab = "k values")
}

}
