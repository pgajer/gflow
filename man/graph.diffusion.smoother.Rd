% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_diffusion_smoother.R
\name{graph.diffusion.smoother}
\alias{graph.diffusion.smoother}
\title{Graph Diffusion Smoother}
\usage{
graph.diffusion.smoother(
  adj.list,
  weight.list,
  y,
  n.time.steps,
  step.factor,
  binary.threshold = 0.5,
  ikernel = 1,
  dist.normalization.factor = 1.1,
  n.CVs = 0,
  n.CV.folds = 10,
  epsilon = 1e-10,
  verbose = FALSE,
  seed = 0
)
}
\arguments{
\item{adj.list}{A list where each element contains neighbor indices.}

\item{weight.list}{A list where each element contains edge weights.}

\item{y}{A numeric vector of values to smooth.}

\item{n.time.steps}{Number of diffusion steps. Default is 100.}

\item{step.factor}{Step size factor (0, 1). Default is 0.5.}

\item{binary.threshold}{Threshold for binary classification. Default is 0.5.}

\item{ikernel}{Kernel type (0-4). Default is 1.}

\item{dist.normalization.factor}{Distance normalization > 1. Default is 1.1.}

\item{n.CVs}{Number of CV runs. Default is 0.}

\item{n.CV.folds}{Number of CV folds. Default is 10.}

\item{epsilon}{Numerical tolerance. Default is 1e-10.}

\item{verbose}{Print progress information. Default is FALSE.}

\item{seed}{Random seed. Default is 0.}
}
\value{
A list containing smoothing results and CV information.
}
\description{
A simplified interface for graph diffusion smoothing with cross-validation.
This function provides a more accessible API while maintaining full functionality.
}
\examples{
\dontrun{
# Create simple chain graph
adj.list <- list(2, c(1,3), c(2,4), c(3,5), 4)
weight.list <- lapply(adj.list, function(x) rep(1, length(x)))
y <- c(1, 0, 1, 0, 1) + rnorm(5, 0, 0.1)

result <- graph.diffusion.smoother(
  adj.list = adj.list,
  weight.list = weight.list,
  y = y,
  n.time.steps = 20,
  n.CVs = 3,
  verbose = TRUE
)
}

}
