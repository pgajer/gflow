% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bias_utils.R
\name{get.magelo.MAB}
\alias{get.magelo.MAB}
\title{Estimate Mean Absolute Bias (MAB) of MAGELO Model}
\usage{
get.magelo.MAB(x, y, xt, yt, deg = 2, y.binary = FALSE, n.cores = 10)
}
\arguments{
\item{x}{A numeric vector of predictor values.}

\item{y}{A numeric vector of response values.}

\item{xt}{A numeric vector of true predictor values for evaluation.}

\item{yt}{A numeric vector of true response values for evaluation.}

\item{deg}{The degree of the MAGELO polynomial model (default = 2).}

\item{y.binary}{Logical indicating if y is a binary variable (default = FALSE).}

\item{n.cores}{Number of cores to use for parallel computation (default = 10).}
}
\value{
A list containing:
\describe{
\item{MAB}{Mean Absolute Bias}
\item{RMSE}{Root Mean Square Error}
\item{predictions}{Predicted values at xt}
\item{residuals}{Absolute residuals}
\item{parameters}{List containing the degree used}
}
}
\description{
Estimates the Mean Absolute Bias (MAB) of a MAGELO model.
}
\examples{
\dontrun{
# Generate example data
set.seed(123)
x <- runif(100, 0, 10)
y <- sin(x) + rnorm(100, sd = 0.1)
xt <- seq(0, 10, length.out = 50)
yt <- sin(xt)

# Fit magelo model and compute MAB
result <- get.magelo.MAB(x, y, xt, yt, deg = 2)
print(result$MAB)
}

}
