% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/major_arms.R
\name{detect.major.arms}
\alias{detect.major.arms}
\title{Detect Major Arms in a Weighted Graph}
\usage{
detect.major.arms(
  adj.list,
  weight.list,
  core.quantile = 0.1,
  use.approx.eccentricity = TRUE,
  n.landmarks = 64L,
  min.arm.size = 50L,
  min.persistence.quantile = 0.8,
  min.length.quantile = 0.8,
  max.arms = NULL,
  seed = 1L,
  verbose = FALSE
)
}
\arguments{
\item{adj.list}{Graph adjacency list (1-based vertex indices).}

\item{weight.list}{Edge-length list aligned with \code{adj.list}.}

\item{core.quantile}{Numeric in (0,1) defining the low-eccentricity core.}

\item{use.approx.eccentricity}{Logical; if \code{TRUE}, uses landmark approximation.}

\item{n.landmarks}{Integer >= 1; number of landmarks for approximation.}

\item{min.arm.size}{Integer >= 1; minimum number of vertices assigned to a tip.}

\item{min.persistence.quantile}{Numeric in the closed interval \verb{[0, 1]};
quantile threshold over tip persistence.}

\item{min.length.quantile}{Numeric in the closed interval \verb{[0, 1]};
quantile threshold over tip length.}

\item{max.arms}{Optional positive integer cap on selected arms.}

\item{seed}{Integer seed for landmark initialization.}

\item{verbose}{Logical; print C++ progress.}
}
\value{
A list of class \code{major_arms} with selected arm tips and diagnostics.
Major components include:
\describe{
\item{major.arms}{Selected arm tips (1-based).}
\item{summary}{Per-vertex diagnostics table (1-based indices).}
\item{arm.table}{Per-tip diagnostics table and selection flags.}
}
}
\description{
Identifies major arms using a geometry-only strategy:
core-by-eccentricity, distance-to-core, peak persistence, and branch mass.
}
