% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_utils.R
\name{generate.graph.gaussian.mixture}
\alias{generate.graph.gaussian.mixture}
\title{Generate a Gaussian Mixture Function on a Graph}
\usage{
generate.graph.gaussian.mixture(
  adj.list,
  weight.list,
  centers,
  amplitudes = rep(1, length(centers)),
  sigmas = rep(2, length(centers)),
  normalize = TRUE
)
}
\arguments{
\item{adj.list}{List of adjacency lists, where \code{adj.list[[i]]} contains indices of
vertices adjacent to vertex \code{i}.}

\item{weight.list}{List of edge weights, where \code{weight.list[[i]][j]} is the weight
of the edge from vertex \code{i} to \code{adj.list[[i]][j]}.}

\item{centers}{Vector of vertex indices to use as centers for the Gaussian components}

\item{amplitudes}{Vector of amplitudes for each Gaussian component}

\item{sigmas}{Vector of sigma values (spread) for each Gaussian component}

\item{normalize}{Logical; if TRUE, normalize the resulting function to \code{[0,1]}.}
}
\value{
A numeric vector of function values at each vertex of the graph
}
\description{
Creates a function on a graph that is a mixture of Gaussian-like components centered
at specified vertices. Each component follows a decay based on the shortest path
distance from its center.
}
\examples{
\dontrun{
# Create a grid graph
grid <- create.grid.graph(10, 10)

# Generate a mixture of two Gaussians
centers <- c(1, 100)  # Corner and center vertices
amplitudes <- c(1.0, 0.7)
sigmas <- c(2.0, 3.0)

y <- generate.graph.gaussian.mixture(
  grid$adj.list,
  grid$weight.list,
  centers,
  amplitudes,
  sigmas
)
}

}
