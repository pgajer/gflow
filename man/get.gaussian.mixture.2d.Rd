% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthetic_data_utils.R
\name{get.gaussian.mixture.2d}
\alias{get.gaussian.mixture.2d}
\title{Generate 2D Gaussian Mixture}
\usage{
get.gaussian.mixture.2d(
  n.components = 3,
  x.range = c(0, 1),
  y.range = c(0, 1),
  centers.strategy = c("random", "grid", "custom"),
  custom.centers = NULL,
  amplitudes.strategy = c("mixed", "positive", "custom"),
  custom.amplitudes = NULL,
  amplitude.range = c(0.5, 2),
  sd.strategy = c("fixed", "random", "custom"),
  sd.value = 0.1,
  sd.range = c(0.05, 0.2),
  custom.sds = NULL,
  correlation = 0,
  edge.buffer = 0.1,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{n.components}{Integer specifying the number of Gaussian components}

\item{x.range}{Numeric vector c(min, max) specifying the range for x values}

\item{y.range}{Numeric vector c(min, max) specifying the range for y values}

\item{centers.strategy}{Character string specifying how to position components:
\itemize{
\item "random": uniformly random positions (default)
\item "grid": regular grid arrangement
\item "custom": user-specified centers
}}

\item{custom.centers}{Matrix with n.components rows and 2 columns (x, y coordinates)
when centers.strategy = "custom"}

\item{amplitudes.strategy}{Character string specifying amplitude generation:
\itemize{
\item "mixed": positive and negative amplitudes (default)
\item "positive": only positive amplitudes
\item "custom": user-specified amplitudes
}}

\item{custom.amplitudes}{Numeric vector of length n.components when
amplitudes.strategy = "custom"}

\item{amplitude.range}{Numeric vector c(min, max) for amplitude magnitudes}

\item{sd.strategy}{Character string specifying how to generate component SDs:
\itemize{
\item "fixed": constant SD for all components (default)
\item "random": random SDs within range
\item "custom": user-specified SDs
}}

\item{sd.value}{Numeric specifying SD when sd.strategy = "fixed"}

\item{sd.range}{Numeric vector c(min, max) when sd.strategy = "random"}

\item{custom.sds}{Numeric vector or 2x2 matrix. If vector, creates isotropic
Gaussians; if matrix, specifies covariance for each component}

\item{correlation}{Numeric between -1 and 1 specifying correlation between
x and y for all components (ignored if custom.sds is a matrix)}

\item{edge.buffer}{Numeric between 0 and 0.5 specifying buffer from edges}

\item{seed}{Optional integer for random number generation}

\item{verbose}{Logical indicating whether to print generation details}
}
\value{
An object of class "gaussian_mixture" containing:
\itemize{
\item \code{f}: Function f(x, y) that evaluates the mixture at given coordinates
\item \code{n.components}: Number of components
\item \code{centers}: Matrix of component centers (n.components x 2)
\item \code{amplitudes}: Vector of component amplitudes
\item \code{covariances}: List of 2x2 covariance matrices for each component
\item \code{x.range}: X range used
\item \code{y.range}: Y range used
\item \code{parameters}: List of all parameters used
}
}
\description{
Creates a 2D Gaussian mixture function with flexible component placement and characteristics.
Returns an object of class "gaussian_mixture" with associated plotting methods.
}
\examples{
\dontrun{
# Generate random 2D mixture
gm1 <- get.gaussian.mixture.2d(n.components = 3)
plot(gm1)

# Generate grid arrangement with custom amplitudes
gm2 <- get.gaussian.mixture.2d(
  n.components = 4,
  centers.strategy = "grid",
  amplitudes.strategy = "custom",
  custom.amplitudes = c(1, -0.5, 0.8, -0.3)
)

# Custom centers with anisotropic Gaussians
gm3 <- get.gaussian.mixture.2d(
  n.components = 2,
  centers.strategy = "custom",
  custom.centers = matrix(c(0.3, 0.3, 0.7, 0.7), ncol = 2, byrow = TRUE),
  correlation = 0.5
)
}
}
