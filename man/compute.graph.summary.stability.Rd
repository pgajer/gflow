% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_summary_divergence.R
\name{compute.graph.summary.stability}
\alias{compute.graph.summary.stability}
\title{Compute graph-summary stability across a graph sequence}
\usage{
compute.graph.summary.stability(
  graphs,
  summary = c("degree_distribution", "edge_weight_distribution",
    "component_size_distribution", "neighborhood_label_distribution"),
  divergence = c("js"),
  labels = NULL,
  summary.args = list(),
  k.values = NULL,
  graph.type = c("geom", "isize"),
  return.details = TRUE
)
}
\arguments{
\item{graphs}{Either a list of graph-like objects or an object of class
\code{"iknn_graphs"}.}

\item{summary}{Summary type to compare across consecutive graph pairs.}

\item{divergence}{Divergence type. Currently only \code{"js"}.}

\item{labels}{Optional shared vertex-label vector used for
\code{"neighborhood_label_distribution"}.}

\item{summary.args}{Optional named list forwarded to
\code{\link[=graph.summary.divergence]{graph.summary.divergence()}}.}

\item{k.values}{Optional integer vector of k values. If \code{graphs} is an
\code{"iknn_graphs"} object and \code{k.values} is \code{NULL}, the values
are derived from its \code{kmin} and \code{kmax} attributes.}

\item{graph.type}{If \code{graphs} is an \code{"iknn_graphs"} object, choose
either \code{"geom"} or \code{"isize"}.}

\item{return.details}{Logical; if \code{TRUE}, return a structured list.}
}
\value{
If \code{return.details = TRUE}, a list with fields:
\itemize{
\item \code{summary}: summary type used.
\item \code{divergence}: divergence type used.
\item \code{k.values}: full \code{k} sequence.
\item \code{k.transition.values}: \code{k} values corresponding to consecutive-graph
transitions.
\item \code{values}: vector of divergence values of length \code{length(k.values) - 1}.
\item \code{details}: optional pairwise details for each transition.
}

Otherwise, returns only the divergence vector.
}
\description{
Computes a divergence-based stability curve across consecutive graphs in a
sequence. This function provides a generalized sequence-level analogue of the
degree-profile Jensen-Shannon calculation currently used in
\code{\link[=compute.stability.metrics]{compute.stability.metrics()}}.
}
