% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_summary_divergence.R
\name{compute.graph.summary.pmf}
\alias{compute.graph.summary.pmf}
\title{Compute a graph summary as a probability mass function}
\usage{
compute.graph.summary.pmf(
  graph,
  summary = c("degree_distribution", "edge_weight_distribution",
    "component_size_distribution", "neighborhood_label_distribution"),
  labels = NULL,
  weight.list = NULL,
  bins = NULL,
  n.bins = 20L,
  bin.method = c("auto", "fixed_width", "fixed_quantile", "explicit"),
  normalize = TRUE,
  support = NULL,
  zero.pad = TRUE,
  simplify.multiple = TRUE,
  directed = FALSE,
  return.details = TRUE
)
}
\arguments{
\item{graph}{Graph-like object. Supported inputs are:
\itemize{
\item an object with \code{adj_list} and optional \code{weight_list}
\item an object with \code{pruned_adj_list} and optional \code{pruned_weight_list}
\item a plain adjacency list (list of integer vectors)
}}

\item{summary}{Summary type to compute.}

\item{labels}{Optional label vector used for
\code{"neighborhood_label_distribution"}. Must have length equal to the
number of vertices.}

\item{weight.list}{Optional edge-weight list used when \code{graph} is a
plain adjacency list.}

\item{bins}{Optional numeric vector of histogram breaks used for
\code{"edge_weight_distribution"}.}

\item{n.bins}{Integer number of bins used when \code{bins} is \code{NULL}
and the summary is \code{"edge_weight_distribution"}.}

\item{bin.method}{Character string controlling automatic bin construction for
\code{"edge_weight_distribution"}.}

\item{normalize}{Logical; if \code{TRUE}, convert counts to a PMF.}

\item{support}{Optional support to align the resulting PMF to.}

\item{zero.pad}{Logical; if \code{TRUE} and \code{support} is provided,
missing support entries are padded with zero mass.}

\item{simplify.multiple}{Logical; if \code{TRUE}, duplicate edges are
collapsed before computing summaries.}

\item{directed}{Logical; if \code{FALSE} (default), treat edges as
undirected.}

\item{return.details}{Logical; if \code{TRUE}, return a structured list.
Otherwise return the named PMF directly.}
}
\value{
If \code{return.details = TRUE}, a list with fields:
\itemize{
\item \code{summary}: summary type used.
\item \code{pmf}: named probability mass function.
\item \code{support}: support of the PMF.
\item \code{counts}: named raw-count vector before normalization.
\item \code{metadata}: list of summary-specific metadata.
}

Otherwise, returns the named PMF vector.
}
\description{
Converts a graph-like object into a named probability mass function (PMF)
describing one selected graph summary. This provides a standardized bridge
between graph objects and divergence functions such as
\code{\link[=jensen.shannon.divergence]{jensen.shannon.divergence()}}.

Supported v1 summaries are:
\itemize{
\item \code{"degree_distribution"}
\item \code{"edge_weight_distribution"}
\item \code{"component_size_distribution"}
\item \code{"neighborhood_label_distribution"}
}

\code{"component_size_distribution"} is included primarily as a diagnostic
for disconnectedness / fragmentation, not as a general-purpose default
stability summary.
}
