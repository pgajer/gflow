% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_utils.R
\name{cont.segments3d}
\alias{cont.segments3d}
\title{Add 3D Line Segments Color-Coded by Continuous Variable}
\usage{
cont.segments3d(X, y, offset, ...)
}
\arguments{
\item{X}{A matrix or data.frame with 3 columns representing 3D coordinates.}

\item{y}{A named continuous numeric vector.}

\item{offset}{Numeric vector of length 3 specifying the offset for line segments.}

\item{...}{Additional arguments passed to \code{\link[rgl]{segments3d}}.}
}
\value{
Invisibly returns NULL.
}
\description{
Adds line segments to a 3D plot with colors determined by a continuous variable
}
\details{
This function adds line segments to an existing 3D plot where each segment's
color is determined by the rank of the corresponding y value. Colors range
from red to violet following the rainbow spectrum.
}
\examples{
\dontrun{
X <- matrix(rnorm(300), ncol = 3)
y <- runif(100)
names(y) <- rownames(X) <- paste0("Sample", 1:100)

plot3D.plain(X)
cont.segments3d(X, y, offset = c(0, 0, 0.1))
}

}
