% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_generators.R
\name{plot.star_object}
\alias{plot.star_object}
\title{Visualize 2D Star Graph with Smooth Function Values}
\usage{
\method{plot}{star_object}(
  x,
  y = NULL,
  point.size = 1.5,
  edge.col = "gray70",
  edge.lwd = 1,
  title = "",
  color.palette = c("blue", "yellow", "red"),
  color.edges = FALSE,
  circle.color = "gray",
  pt.lab.adj = c(1, 1),
  pt.lab.cex = 0.75,
  grid.pt.lab.adj = c(1, 1),
  grid.pt.lab.cex = 0.75,
  grid.pt.color = "cyan",
  grid.pt.text.color = "cyan",
  grid.pt.cex.factor = 1.5,
  show.vertex.labs = FALSE,
  zero.based = FALSE,
  show.arm.labs = TRUE,
  arm.cex = 2,
  arms.adj = c(-0.5, 1),
  arm.label.offset = 0,
  show.mu = TRUE,
  mu.cex = 3,
  epsilon = 0.1,
  legend.inset = 0.05,
  legend.bty = "n",
  legend.title = "Function Value",
  axes = FALSE,
  xlab = "",
  ylab = "",
  gpd.obj = NULL,
  gpd.path.index = NULL,
  ugg.obj = NULL,
  ...
)
}
\arguments{
\item{x}{A star_object (output from generate.star.dataset function)}

\item{y}{Optional numeric vector. If not NULL, it has have the same length as graph.data$y.smooth.}

\item{point.size}{Numeric. Size of vertex points (default = 1.5)}

\item{edge.col}{Character. Color of edges when not using color.edges (default = "gray70")}

\item{edge.lwd}{Numeric. Line width for edges (default = 1)}

\item{title}{Character. Plot title (default = "")}

\item{color.palette}{Character vector. Colors for the gradient (default = c("blue", "yellow", "red"))}

\item{color.edges}{Logical. If TRUE, edges are colored based on average function
values of connected vertices (default = FALSE)}

\item{circle.color}{Character. Color of the reference circle (default = "gray")}

\item{pt.lab.adj}{Numeric vector of length 2. Text adjustment for vertex labels (default = c(1.0, 1.0))}

\item{pt.lab.cex}{Numeric. Character expansion factor for vertex labels (default = 0.75)}

\item{grid.pt.lab.adj}{Numeric vector of length 2. Text adjustment for grid point labels (default = c(1.0, 1.0))}

\item{grid.pt.lab.cex}{Numeric. Character expansion factor for grid point labels (default = 0.75)}

\item{grid.pt.color}{Character. Color for grid points (default = "cyan")}

\item{grid.pt.text.color}{Character. Color for grid point text labels (default = "cyan")}

\item{grid.pt.cex.factor}{Numeric. Size factor for grid points (default = 1.5)}

\item{show.vertex.labs}{Logical. Whether to show vertex labels (default = FALSE)}

\item{zero.based}{Logical. If TRUE, print vertex labels in 0-based format; otherwise 1-based (default = FALSE)}

\item{show.arm.labs}{Logical. Whether to show arm labels (default = TRUE)}

\item{arm.cex}{Numeric. Character expansion factor for arm labels (default = 2)}

\item{arms.adj}{Numeric vector of length 2. Text adjustment for arm labels (default = c(-0.5, 1))}

\item{arm.label.offset}{Numeric. It controls label distance from arm end.}

\item{show.mu}{Logical. Whether to show the function center point (default = TRUE)}

\item{mu.cex}{Numeric. Character expansion factor for function center point (default = 3)}

\item{epsilon}{Numeric. Margin factor for plot boundaries (default = 0.1)}

\item{legend.inset}{Numeric. Inset factor for legend position (default = 0.05)}

\item{legend.bty}{Character. Box type for legend ("o" for box, "n" for no box) (default = "n")}

\item{legend.title}{Character. Title for the color scale legend (default = "Function Value")}

\item{axes}{Logical. Whether to show plot axes (default = FALSE)}

\item{xlab}{Character. Label for x-axis (default = "")}

\item{ylab}{Character. Label for y-axis (default = "")}

\item{gpd.obj}{List. Optional path data for highlighting vertices. Each element should be
a list containing $vertices (vector of vertex indices) and optionally $ref_vertex
(index of reference vertex) (default = NULL)}

\item{gpd.path.index}{Integer. Index of the path to highlight from gpd.obj (default = NULL)}

\item{ugg.obj}{Object. Uniform grid graph object for additional visualization (default = NULL)}

\item{...}{Additional graphical parameters (currently unused)}
}
\value{
NULL (generates a plot as a side effect)
}
\description{
Creates a visualization of a 2D star graph where vertices are colored
according to the values of the smooth function. The visualization includes
a reference circle, edges connecting vertices, and optional path highlighting.
Vertices can be labeled and the function center (mu) can be displayed.
}
\details{
The function creates a visualization where vertices are arranged according to the star
graph structure. Vertices are colored based on their function values using the specified
color palette. A reference circle is drawn to show the maximum arm length. When gpd.obj
is provided, vertices in paths are highlighted with red circles and the reference vertex
(if specified) is highlighted with a larger blue circle.
}
\examples{
# Generate basic star graph data
data <- generate.star.dataset(
  n.points = 20,
  n.arms = 5,
  min.arm.length = 1,
  max.arm.length = 3,
  dim = 2,
  fn = "exp",
  noise = "norm",
  noise.sd = 0.1
)

# Basic visualization
plot(data)

# Visualization with custom styling and path highlighting
path_data <- list(
  list(vertices = c(1, 2, 3), ref_vertex = 1)
)
plot(data,
  point.size = 2,
  color.edges = TRUE,
  show.vertex.labs = TRUE,
  gpd.obj = path_data
)

}
