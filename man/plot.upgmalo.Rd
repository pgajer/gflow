% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pgmalo.R
\name{plot.upgmalo}
\alias{plot.upgmalo}
\title{Plot Method for upgmalo Objects}
\usage{
\method{plot}{upgmalo}(
  x,
  type = c("fit", "diagnostic", "residuals", "residuals_hist"),
  title = "",
  xlab = "",
  ylab = "",
  predictions.type = c("predictions", "bb.predictions", "local.predictions"),
  with.y.true = TRUE,
  with.pts = FALSE,
  with.CrI = TRUE,
  y.true.col = "red",
  predictions.col = "blue",
  with.predictions.pts = FALSE,
  predictions.pts.col = "blue",
  predictions.pts.pch = 20,
  CrI.as.polygon = TRUE,
  CrI.polygon.col = "gray85",
  CrI.line.col = "gray10",
  CrI.line.lty = 2,
  ylim = NULL,
  ...
)
}
\arguments{
\item{x}{An object of class "upgmalo" returned by \code{\link{upgmalo}}.}

\item{type}{Character string specifying the plot type:
\describe{
\item{"fit"}{Fitted values with optional confidence bands (default)}
\item{"diagnostic"}{Cross-validation error by neighborhood size}
\item{"residuals"}{Residual scatter plot}
\item{"residuals_hist"}{Histogram of residuals}
}}

\item{title}{Main title for the plot (default: "").}

\item{xlab}{X-axis label (default: "x" for most plots).}

\item{ylab}{Y-axis label (default: "y" for fit plot, appropriate label for others).}

\item{predictions.type}{Character string specifying which predictions to plot:
\describe{
\item{"predictions"}{Global optimal h predictions (default)}
\item{"bb.predictions"}{Bootstrap-based predictions}
\item{"local.predictions"}{Locally adaptive predictions}
}}

\item{with.y.true}{Logical; include true values if available (default: TRUE).}

\item{with.pts}{Logical; show original data points (default: FALSE).}

\item{with.CrI}{Logical; show credible/confidence intervals if available (default: TRUE).}

\item{y.true.col}{Color for true values line (default: "red").}

\item{predictions.col}{Color for predictions line (default: "blue").}

\item{with.predictions.pts}{Logical; show prediction points (default: FALSE).}

\item{predictions.pts.col}{Color for prediction points (default: "blue").}

\item{predictions.pts.pch}{Point character for predictions (default: 20).}

\item{CrI.as.polygon}{Logical; show confidence band as polygon (default: TRUE).}

\item{CrI.polygon.col}{Color for confidence band polygon (default: "gray85").}

\item{CrI.line.col}{Color for confidence band lines if not polygon (default: "gray10").}

\item{CrI.line.lty}{Line type for confidence bands if not polygon (default: 2).}

\item{ylim}{Y-axis limits (default: NULL for automatic).}

\item{...}{Additional graphical parameters passed to base plotting functions.}
}
\value{
NULL (invisibly). Called for side effect of creating plots.
}
\description{
Creates comprehensive visualizations for univariate PGMALO results including
fitted values with confidence bands, diagnostic plots, and residual analysis.
}
\details{
The function provides multiple visualization options:
\itemize{
\item Fit plots show the estimated function with optional confidence bands
\item Diagnostic plots help assess the cross-validation process
\item Residual plots aid in model checking
}

Confidence intervals are only shown for "predictions" and "bb.predictions"
types when bootstrap was performed (n.bb > 0 in the original call).
}
\examples{
\dontrun{
# Generate example data
n <- 100
x <- seq(0, 2*pi, length.out = n)
y <- sin(x) + rnorm(n, 0, 0.2)

# Fit model
fit <- upgmalo(x, y, n.bb = 100)

# Create various plots
plot(fit, type = "fit", with.pts = TRUE)
plot(fit, type = "diagnostic")
plot(fit, type = "residuals")
}

}
\seealso{
\code{\link{upgmalo}} for model fitting,
\code{\link{summary.upgmalo}} for numerical summaries
}
