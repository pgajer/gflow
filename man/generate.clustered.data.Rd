% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthetic_data_utils.R
\name{generate.clustered.data}
\alias{generate.clustered.data}
\title{Generates clustered data (diagonal covariance)}
\usage{
generate.clustered.data(
  n.clusters,
  cluster.sizes,
  dimensions = 2,
  means = NULL,
  covariances = NULL,
  separation = 2
)
}
\arguments{
\item{n.clusters}{Integer, number of clusters.}

\item{cluster.sizes}{Integer vector of length n.clusters with sizes per cluster.}

\item{dimensions}{Integer, number of dimensions (columns).}

\item{means}{Optional numeric matrix \code{[n.clusters x dimensions]} of cluster means.}

\item{covariances}{Optional list of length n.clusters; each element either
(i) a diagonal covariance matrix \code{(dimensions x dimensions)},
or (ii) a numeric vector of variances of length \code{dimensions}.
If NULL, uses 0.5 * I.}

\item{separation}{Numeric, separation scale used when \code{means} is NULL.}
}
\value{
Numeric matrix with \code{sum(cluster.sizes)} rows and \code{dimensions} columns.
}
\description{
Generates clustered data (diagonal covariance)
}
