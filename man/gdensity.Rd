% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/density.R
\name{gdensity}
\alias{gdensity}
\title{Kernel Density Estimation on a Uniform Grid}
\usage{
gdensity(
  x,
  grid.size = 512L,
  poffset = 0.1,
  bw = 0,
  kernel = 5L,
  verbose = FALSE
)
}
\arguments{
\item{x}{Numeric vector of observations}

\item{grid.size}{Integer specifying the number of points in the estimation grid (default: 512)}

\item{poffset}{Numeric value between 0 and 1 specifying the proportion of data range
to add as padding on each end (default: 0.1)}

\item{bw}{Bandwidth for kernel estimation. If <= 0, bandwidth is automatically selected
(default: 0)}

\item{kernel}{Integer specifying kernel type:
1 = Epanechnikov,
2 = Triangular,
4 = Laplace,
5 = Normal,
6 = Biweight,
7 = Tricube}

\item{verbose}{Logical indicating whether to print progress information (default: FALSE)}
}
\value{
A list containing:
\itemize{
\item density - Vector of density estimates at grid points
\item bw - Bandwidth used for estimation
\item bw_auto_selected - Logical indicating if bandwidth was automatically selected
\item offset - Actual offset added to data range
\item start - Starting point of the grid
\item end - End point of the grid
}
}
\description{
Estimates probability density using kernel density estimation on a uniform grid.
Similar to base R's density() but uses a fixed uniform grid for estimation.
}
\examples{
data <- rnorm(100)
result <- gdensity(data, grid.size = 200, poffset = 0.1, bw = 0, kernel = 5)
plot(result$x, result$density, type = "l")

}
