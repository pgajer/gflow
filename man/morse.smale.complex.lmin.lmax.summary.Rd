% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_MS_cx.R
\name{morse.smale.complex.lmin.lmax.summary}
\alias{morse.smale.complex.lmin.lmax.summary}
\title{Process Complete Morse-Smale Complex Analysis Pipeline}
\usage{
morse.smale.complex.lmin.lmax.summary(
  MS.res,
  state.space,
  taxonomy,
  rel.condE,
  freq.thld = 100,
  min.relAb.thld = 0.05,
  outcome.name = "outcome"
)
}
\arguments{
\item{MS.res}{List containing Morse-Smale complex results with component MS_cx}

\item{state.space}{Matrix representing the state space (e.g., ASV abundance matrix)}

\item{taxonomy}{Taxonomy information for state space features}

\item{rel.condE}{Numeric vector of relative conditional expectations}

\item{freq.thld}{Frequency threshold for filtering (default: 100)}

\item{min.relAb.thld}{Minimum relative abundance threshold for labels (default: 0.05)}

\item{outcome.name}{Character string describing the outcome variable (default: "outcome")}
}
\value{
A list with four components:
\item{labels}{Results from create.lmin.lmax.labels including lmin.labels and lmax.labels}
\item{indicators}{Results from create.lmin.lmax.label.indicators including indicator vectors and tables}
\item{frequencies}{Results from create.lmin.lmax.frequency.tables including frequency tables and captions}
\item{contingency}{Results from create.lmin.lmax.contingency.table including contingency table and caption}
}
\description{
Executes a complete analysis pipeline for Morse-Smale complex data by sequentially
processing critical points, creating labels, generating indicators, computing
frequency tables, and forming contingency relationships.
}
\details{
The function executes the following steps:
\enumerate{
\item Creates labels for local minima and maxima based on taxonomic composition
\item Generates indicator vectors and label tables for critical points
\item Computes frequency tables with statistics for critical points
\item Creates contingency table showing relationships between local minima and maxima
}
}
\examples{
\dontrun{
results <- morse.smale.complex.lmin.lmax.summary(
    MS.res = smoothed.condE.MS.res,
    state.space = asv.matrix,
    taxonomy = asv.taxonomy,
    rel.condE = rel.smoothed.condE,
    outcome.name = "smoothed conditional expectation"
)
}
}
