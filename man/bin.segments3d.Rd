% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_utils.R
\name{bin.segments3d}
\alias{bin.segments3d}
\title{Add 3D Line Segments for Binary Variable}
\usage{
bin.segments3d(
  X,
  y,
  offset,
  with.labels = TRUE,
  lab.tbl = NULL,
  lab.adj = c(0, 0),
  lab.cex = 1,
  C = 1,
  ...
)
}
\arguments{
\item{X}{A matrix or data.frame with 3 columns representing 3D coordinates.}

\item{y}{A named binary (0/1) vector.}

\item{offset}{Numeric vector of length 3 specifying the offset for line segments.}

\item{with.labels}{Logical. Whether to show labels for y=1 positions.}

\item{lab.tbl}{Named vector mapping sample IDs to labels.}

\item{lab.adj}{Adjustment parameter for label positioning.}

\item{lab.cex}{Character expansion factor for labels.}

\item{C}{Scaling factor for label position relative to stick center.}

\item{...}{Additional arguments passed to \code{\link[rgl]{segments3d}}.}
}
\value{
Invisibly returns NULL.
}
\description{
Adds vertical line segments to a 3D plot at positions where a binary variable equals 1
}
\details{
This function adds vertical line segments (sticks) to an existing 3D plot
at positions where the binary variable y equals 1. Optionally, labels can
be added above the sticks.
}
\examples{
\dontrun{
X <- matrix(rnorm(300), ncol = 3)
y <- sample(0:1, 100, replace = TRUE)
names(y) <- rownames(X) <- paste0("Sample", 1:100)

plot3D.plain(X)
bin.segments3d(X, y, offset = c(0, 0, 0.1))
}

}
