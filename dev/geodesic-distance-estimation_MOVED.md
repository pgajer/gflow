# Geodesic Distance Estimation Assets Moved

The noisy-circle and density-aware geodesic distance estimation experiment
assets that were previously tracked under `gflow/dev/geodesic-distance-estimation/`
now live in:

`/Users/pgajer/current_projects/dgraphs_manuscripts/geodesic_data_geometry/experiments/geodesic-distance-estimation/`

The `dgraphs` repository is now the source of truth for reusable graph package
implementation and tests. Experiment caches and `.rds` outputs are derived
artifacts and should remain outside version control.

Older recovered reports and sources are retained separately in the manuscript
workspace's `archive/gflow-20260912/geodesic-distance-estimation/` directory.
